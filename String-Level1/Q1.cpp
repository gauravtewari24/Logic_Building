#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

#define f(i, a, b) for (ll i = a; i < b; i++)

int main()
{
    ll t, n, a = 0, b = 0, i, j, k, c, sum;
    string s;
    cin >> s;
    cout << s;
}
