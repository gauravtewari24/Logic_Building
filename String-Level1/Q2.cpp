#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

#define f(i, a, b) for (ll i = a; i < b; i++)

int main()
{
    ll t, n, a = 0, b = 0, i, j, k, c, sum;
    string f, l, m;
    cin >> f >> m >> l;
    cout << f << " " << m << " " << l << "- " << f[0] << "." << m[0] << "." << l[0];
}
